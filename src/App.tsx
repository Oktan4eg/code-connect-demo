import './App.css';
import { GetVariables } from './Json-to-css/getVariables';

function App() {
  return (
    <div>
      Hello world!
      <GetVariables />
    </div>
  );
}

export default App;

// let colorVariables: Array<ColorType> = [];

// var index, len;
// for (index = 0, len = colorVariables.length; index < len; ++index) {
//   console.log(colorVariables[index]);
// }

//console.log(colorVariables[0]);
